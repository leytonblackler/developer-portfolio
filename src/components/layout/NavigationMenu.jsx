import React from "react";
import styled from "styled-components";

const NavigationMenuContainer = styled.div`
  width: 100vw;
  height: 100vh;
  position: fixed;
  /* background-color: orange; */
`;

export default () => (
  <NavigationMenuContainer>navigation menu here</NavigationMenuContainer>
);
