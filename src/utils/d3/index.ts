import { interpolate, select, selectAll } from "d3";
import {
  forceCollide,
  forceManyBody,
  forceSimulation,
  forceX,
  forceY,
} from "d3-force";

export const d3 = {
  select,
  selectAll,
  interpolate,
  forceSimulation,
  forceCollide,
  forceManyBody,
  forceX,
  forceY,
};
